# HomeLab PLAN

盆栽のように、小さく作って長く育てる。

> **読み方**: まず **Phase 0 と「最初の一歩」だけ**読んで手を動かす。以降は、その段階に来てから開く。
> 手が止まったら「道場」へ。何を載せるか迷ったら「サービスカタログ」へ。

## ゴール

- 自宅に「壊しても直せるインフラ」を持ち、Linux / OS / ネットワーク / ハードウェアを手を動かして学ぶ
- 最終的に Home Assistant を主役に据える
- **すべての設定をコードで再現できる**（手作業を残さない）
- 運用費は電気代だけ（年 2,000 円台）。**解約したサブスクで収支を黒字にする**

## 原則

1. **手で設定したら負け** — 一度は手で試してよい。動いたら Ansible / Terraform に落とし、消して作り直す
2. **1 フェーズ 1 台** — ハードを増やすのは、今の台数で困ってから
3. **ポートは開けない** — 外からのアクセスは Tailscale。ルータのポート開放はしない
4. **壊す前提** — いつでも OS を入れ直せる状態を保つ。守るのはデータだけ
5. **学びを残す** — 詰まったら `docs/` に 3 行書く。それが homelab の資産
6. **家族が使い始めた瞬間に本番** — 実験は実験用 VM で。本番を壊すと技術ではなく信頼を失う
7. **動いたら終わりにしない** — 1 段下まで説明できるか自問する。できないなら「道場」へ

> **homelab は資産であり負債でもある。** DNS が落ちれば家中のネットが死に、写真を預けたら二度と失えない。
> 得られるのは便利さ以上に、**サブスクとデータの主導権が自分に戻ること**。その対価に運用責任を負う取引だと理解して始める。

---

## この PLAN で身につくもの・身につかないもの

**先に期待値を合わせる。** ここを誤解したまま進めると、1 年後に「思ったより強くなっていない」と感じる。

### 身につく

| 領域 | 度合い | 補足 |
|---|---|---|
| Linux 運用（systemd / ストレージ / パッケージ） | ★★★ | 毎日触るので確実に付く |
| ネットワーク（DNS / VPN / VLAN / パケット） | ★★★ | Phase 1・4 と「可視化」節で実物に触れる |
| IaC（Ansible / Terraform） | ★★★ | **自分の資産で**書ける。壊せるのが最大の利点 |
| 仮想化・コンテナの境界 | ★★★ | KVM / LXC / namespace の違いを実機で |
| 監視の設計 | ★★☆ | exporter を自作するところまで行けば本物 |
| 物理（BIOS / 電力 / 熱 / ディスク寿命） | ★★☆ | **仕事ではまず触れない。ここが差別化になる** |

### 身につかない（homelab に構造的に無いもの）

| 欠けるもの | なぜ取れないか | どこで埋めるか |
|---|---|---|
| **スケール** | 1 台しかない。分散・整合性・シャーディングの問題が発生しない | 仕事。個人なら k3s + 負荷生成で「模擬」まで |
| **トラフィック** | 誰もアクセスしない。SLO もキャパシティプランニングも前提が立たない | 仕事。または公開サービスを 1 個だけ持つ |
| **人と組織** | オンコール、ポストモーテム、SLO の交渉、開発チームとの調整 | **仕事でしか取れない。SRE の中核はここ** |
| **本番の緊張感** | 壊れて困るのは自分だけ | 家族に使わせる（原則 6）が唯一の代替 |
| **クラウドの規模の勘所** | マネージドサービス、IAM 設計、コスト最適化 | 仕事 or 別リポジトリ |

### 正直な結論

- **「これさえやれば強い SRE になれる」→ ならない。** SRE の中核は分散システムと組織であり、homelab はそこに触れない
- **「インフラの土台が付くか」→ 付く。** しかも**仕事だけでは取れない層**（物理・OS・全部自分で決める経験）が取れる
- **「低レイヤーが付くか」→ 半分。** ここで言う低レイヤーは*運用の*低レイヤー（syscall / パケット / ブートシーケンス）。
  CPU アーキテクチャ、コンパイラ、カーネル実装は範囲外。そこへ行きたいなら別の教材が要る
- **本当の価値は「1 段下を見る癖」（原則 7）。** これは仕事の障害対応で直接効く。homelab は**土台**であって、強さは仕事の規模と意思決定でしか付かない。両輪で回す

---

## 全体像

| Phase | ハード | テーマ | 目安 |
|---|---|---|---|
| 0 | 手持ちの Windows PC | Linux VM を建てて SSH で入る | 1 週 |
| 1 | 同上 | Ansible で全コード化、Docker で初サービス | 2〜4 週 |
| 2 | ミニ PC 1 台を購入 | Proxmox + Terraform、監視、24/365 化 | 1〜2 か月 |
| 3 | 同上 + Zigbee ドングル | Home Assistant 本格導入 | 継続 |
| 4 | Raspberry Pi / NAS など | ネットワーク・ストレージ・脱クラウド・ローカル LLM | 気が向いたら |
| 5 | スマートメーター / 太陽光 | 電気・ガス代を下げる | データが溜まってから |

---

## Phase 0 — Windows PC で Linux を建てる（無料）

**目的**: 「LAN 上に IP を持った Linux サーバー」を 1 台手に入れる。

> **Phase 0-1 は 24 時間動かさなくていい。** 常時稼働は Phase 2 から。Windows は勝手に再起動し、うるさく、電気も食う。

### やること

- [ ] `winver` で Windows のエディションを確認する
  - [ ] **Pro / Enterprise** → Hyper-V を有効化。仮想スイッチは「外部」（VM が LAN 上に自分の IP を持つ）
  - [ ] **Home** → VirtualBox で代用。ネットワークは「ブリッジアダプター」
- [ ] Ubuntu Server LTS を VM にインストール（GUI なし / CPU 2 / RAM 4GB / Disk 32GB）
- [ ] ルータの DHCP で、VM の MAC アドレスに固定 IP を予約する
- [ ] SSH 鍵でログインできるようにする（パスワード認証は切る）

### Done

- [ ] `ssh homelab` で鍵だけで入れる
- [ ] VM を一度消して、同じ手順で作り直せた

### 学ぶこと

仮想化とは何か（ハイパーバイザ Type-1 / Type-2）、NAT とブリッジの違い（＝ルータから見て VM が居るか）、SSH 公開鍵認証、`systemd` の起動シーケンス（`systemd-analyze blame`）。

---

## Phase 1 — すべてを Ansible に落とす（無料）

**目的**: Phase 0 の手作業を、**コマンド 1 発で再現できる状態**にする。

### やること

- [ ] `ansible/` を作り、Phase 0 の手作業を playbook 化する
  - [ ] ユーザー / SSH / `ufw` / タイムゾーン / 自動更新（`unattended-upgrades`）
- [ ] Docker を Ansible で入れる
- [ ] **Pi-hole** を Docker Compose で立てる（DNS 広告ブロック。効果が体感でき、DNS を学べる）
- [ ] **Uptime Kuma** を立てる（死活監視。自分のラボを自分で監視する）
- [ ] **Tailscale** を入れ、外出先から Pi-hole 管理画面に入れるようにする（Personal は無料）
- [ ] GitHub Actions で `ansible-lint` と `--check`（dry-run）を回す

### Done

- [ ] VM を作り直し → `ansible-playbook site.yml` だけで元に戻せる
- [ ] 家の全端末の DNS が Pi-hole を向き、広告が消えている

### 学ぶこと

冪等性（何回流しても同じ結果 = Ansible の本質）、DNS の解決順序と `/etc/resolv.conf` と `dig`、Docker のネットワーク / ボリュームと `iptables` への書かれ方、WireGuard がどう NAT を越えるか（Tailscale の中身）。

---

## Phase 2 — ミニ PC を買って Proxmox へ（初めてお金を使う）

**目的**: Windows PC を解放し、24 時間動く本番機を持つ。VM を Terraform で作る。

### やること

- [ ] ミニ PC を 1 台買う（→「ハードウェア選定」）
- [ ] **物理の壁を越える**（初心者が一番詰まるのはソフトではなくここ）
  - [ ] USB メモリに ISO を焼く（**Ventoy** なら ISO をコピーするだけ。**Rufus** でも可）
  - [ ] 電源投入直後に `F2` / `Del` / `F12` で BIOS/UEFI へ入る（機種で違う）
  - [ ] **仮想化支援を有効化**（`Intel VT-x` / `AMD-V` と `VT-d`）。無効だと VM が動かない
  - [ ] **Secure Boot を無効化**。有効だと Proxmox が起動しないことがある
- [ ] **Proxmox VE 9.x** をインストール（Debian 13 ベースの Type-1 ハイパーバイザ、無料）
  - [ ] Enterprise リポジトリを無効化し、No-Subscription に切り替える（初回に必ず踏むエラー）
- [ ] Proxmox に API トークンを作り、**Terraform（`bpg/proxmox`）** で VM / LXC を定義する
  - `telmate/proxmox` は更新が止まりがち。今は `bpg/proxmox` が事実上の標準
- [ ] **Terraform で箱を作る → Ansible で中身を作る**、の 2 段構えにする
- [ ] Phase 1 のサービスを新ホストへ移し、Windows PC の VM は捨てる
- [ ] バックアップを決める（VM は Proxmox Backup、設定・データは `restic` で外付け HDD へ）
- [ ] **名前でアクセスできるようにする** — 内部ドメイン（`*.lab.home` 等）を Pi-hole に登録し、**Caddy** を前段に置く
  - Let's Encrypt の **DNS-01 チャレンジ**なら、外部公開せずに内部サービスも HTTPS にできる（ドメインが要る）
  - ⚠️ DNS-01 は**ドメインの DNS を API で書き換える**方式。ここで DNS プロバイダを持つので、**同じ `terraform/` でクラウドも管理し始める**（→ 該当節）
- [ ] **監視を入れる**（SRE の本業。手を抜かない）
  - [ ] **Prometheus + Grafana + node_exporter** — CPU / メモリ / ディスク / 温度 / 電力
  - [ ] **`smartctl_exporter`** — SSD の書き込み量と健康度。**壊れる前に気づく唯一の手段**
  - [ ] **ntfy** でアラートをスマホへ（Kuma = 外形、Prometheus = 内部指標。役割を分ける）
  - [ ] **自作 exporter を 1 個書く**（→ 道場「自分のコードを 1 個だけ置く」）
- [ ] **Renovate** を GitHub に入れ、イメージ / プロバイダ更新を PR で受ける

### Done

- [ ] `terraform apply` → `ansible-playbook` で、まっさらな Proxmox から全サービスが復活する
- [ ] 電源を抜いて挿し直しても、全部自動で戻ってくる

### 学ぶこと

ベアメタル / KVM / LXC の違い（VM とコンテナは何を共有しているか）、cloud-init、Terraform の state（最初はローカルで十分。壊して学ぶ）、TLS 証明書と DNS-01（なぜ外部公開なしで証明書が取れるのか）、メトリクスの型（Counter / Gauge / Histogram）と PromQL、実測の消費電力（ワットチェッカー 1 個で世界が変わる）。

---

## Phase 3 — Home Assistant を育てる

**目的**: 家が便利になる。ここからが盆栽。

### やること

- [ ] **Home Assistant OS を VM として** Proxmox に建てる（公式サポート形態。Supervisor とアドオンが使える）
- [ ] 既存の家電・Wi-Fi 機器を統合する（買い物ゼロで始まる）
- [ ] **Zigbee USB ドングル**（SLZB-06 / SkyConnect 系, 3,000〜6,000 円）を追加する
- [ ] 温湿度センサーを **1 個だけ**買い、「暑くなったらエアコン」で成功体験を作る
- [ ] 設定を Git 管理する（`configuration.yaml` と自動化。Secrets は分離）
- [ ] 自動バックアップを組む（HA のバックアップ → NAS / 外付けへ）
- [ ] **本番と実験を分ける** — HA / Pi-hole / 写真は本番。新しい遊びは別 VM で試してから入れる
  - 家族が「電気がつかない」と言ったら、それは**インシデント**（原則 6）

### Done

- [ ] HA VM を消して、バックアップから戻せた
- [ ] 自動化が最低 1 つ、毎日勝手に動いている
- [ ] 自分以外の家族が 1 つでも日常的に使っている

### 育て方（急がない）

- センサーは **1 種類ずつ**。増やしたら 1 週間放置して様子を見る
- ダッシュボードは実用から（在室・温湿度・電力）。**電力の可視化が最優先** → Phase 5 の判断材料になる
- **HACS**（コミュニティ製の統合・カードを入れる仕組み）で世界は広がるが、**公式統合で足りないか先に確認**。壊れやすいのはいつも野良側

### サッカーを組み込む

[**Team Tracker**](https://github.com/vasqued2/ha-teamtracker)（HACS。ESPN API からスコアをセンサー化。[専用カード](https://github.com/vasqued2/ha-teamtracker-card)あり）のセンサーは**属性に試合状況を持つ**ので、そのまま自動化のトリガーになる。

- [ ] **キックオフ通知** — 開始 30 分前に ntfy でスマホへ
- [ ] **ゴール演出** — スコア変化で照明をチームカラーにフラッシュ。Zigbee 電球 1 個（2,000 円）。**家族に一番ウケる**
- [ ] **ネタバレ防止モード** ★本命 — 録画で見る間、通知を抑制する。`input_boolean` を 1 個作るだけ
- [ ] **試合日程を HA の Calendar へ** — ダッシュボードに次節を出す

チームカラーが取れないリーグは `teamtracker_overrides.json` で指定する。J リーグは ESPN API のカバー次第なので、**対象リーグが取れるか確認してから**組む。

---

## Phase 4 — 広げる（必要になってから）

やりたくなった順に。**全部やる必要はない。**

- [ ] **脱 iCloud / 自前クラウド** ★一番元が取れる — **Immich**（写真、iOS アプリあり）＋ **Nextcloud**（ファイル同期）。ここで RAID / ZFS / SMART / 3-2-1 を学ぶ
  - ⚠️ 家族の写真 = 消したら終わり。**バックアップが動く確証を得てから** iCloud を解約する
- [ ] **NAS** — 上を動かすなら必須。中古ミニ PC + 外付け HDD で始め、足りなくなったら 4 ベイへ
- [ ] **Raspberry Pi 5 を買い足す** — GPIO / 電子工作、Pi-hole の冗長化（2 台目の DNS）、物理層の実験台
- [ ] **ネットワーク強化** — VLAN で IoT を隔離、OPNsense をルータ化（→「可視化」Step 4）
- [ ] **UPS** — 停電で ZFS を壊す前に。NUT で HA から監視できる
- [ ] **おうち Kubernetes（k3s）** — **最後でいい**。Docker Compose で困ってから。Pi クラスタは k8s の教材としては優秀だが、**サービスを安定させる手段としては遠回り**

生活が良くなる側のサービス（Jellyfin / Frigate / ntfy など）は → **サービスカタログ**に集約。

### ローカル音声アシスタント

- [ ] HA の Assist に **Whisper**（音声認識）+ **Piper**（合成）を載せる
- **クラウドに声を送らずに**「電気消して」が通る。低スペックでも日本語は動く

### ローカル LLM（期待値を先に下げる）

**目的は「ChatGPT を自宅に置く」ことではない。** 性能でもコストでも勝てない。狙いは 2 つだけ。

1. **プロンプトを外に出さない**（家のデータ、HA のエンティティ名、家族の会話）
2. **LLM が動く仕組みを一段下まで理解する**（量子化、KV キャッシュ、ボトルネックはメモリ帯域）

- [ ] **Ollama**（実行）+ **Open WebUI**（UI）を Docker で立てる。どちらも無料
- [ ] HA の公式 **Ollama 統合**で conversation agent にする（Assist と組めば**声も文章もクラウドに出ない**）
  - ⚠️ 家電を操作させるには **tool 呼び出し対応モデル**が必要。小さいモデルほど誤操作する
  - 公式は**公開エンティティ 25 個未満**を推奨。まず「操作させない・雑談だけ」で始める（原則 6）

> **量子化**: モデルの重みを 16bit → 4bit に丸め、サイズを 1/4 にすること。精度は少し落ちるが **RAM に載る**ようになる。載らないとディスクを読みに行って 100 倍遅くなるので、載せることが最優先。

| 構成 | 動くモデル | アイドル | 推論中 | 年間電気代の増分 |
|---|---|---|---|---|
| N150 の CPU のみ | 3B〜8B（4bit）・数 token/秒 | 7〜8W | 15〜20W | ほぼゼロ |
| 中古 GPU（RTX 3060 12GB 等） | 14B 級まで快適 | **+15〜25W（挿すだけで常時）** | 150W〜 | **+5,000〜8,000 円** |

**効くのはアイドル分。** GPU 1 枚で「収支を黒字にする」目標が単独で吹き飛ぶ。やるなら「学ぶため」と割り切るか、**使う時だけ起動する**設計にする。

#### Done

- [ ] `ollama run` ではなく、**Ansible / Compose で再構築できる**
- [ ] 「なぜこのモデルは動いてあれは動かないか」を、パラメータ数 × 量子化ビット数 ≒ 必要 RAM で説明できる

---

## Phase 5 — 電気・ガス代を下げる（データが溜まってから）

**目的**: 「なんとなく高い」を「どこで何 kWh 使っているか」に変える。**投資判断はその後。**

### 順番を守る（ここが要）

- [ ] **1. 測る** — スマートメーターの **B ルート**（電力会社に申請、無料）を HA に取り込み、30 分粒度で記録
  - Wi-SUN USB ドングル（RS-WFWATTCH / BP35A1 系, 5,000〜10,000 円）が必要
  - ガスはスマートメーターが未普及。当面は検針票を手入力でよい
- [ ] **2. 貯める** — 最低 **1 年分**。夏冬の両方を見ないと判断を誤る
- [ ] **3. 削る（無料）** — 待機電力、給湯・エアコンの運転、料金プラン見直し。**ここで数千円/月落ちることが多い**
- [ ] **4. 削る（自動化）** — HA で「不在なら切る」「安い時間帯に沸かす」
- [ ] **5. 投資を検討** — 上をやり尽くしてから、太陽光 / 蓄電池を**回収年数で**評価する

### 判断の目安

- 太陽光・蓄電池は初期費用 100 万円規模、**回収 10 年前後**。削減額のデータなしでは計算できない
- 小さく試すなら**ベランダ発電**（400W 級 + ポータブル電源, 5〜10 万円）。工事不要で発電の物理を体で学べる
- 賃貸なら固定設置は不可。ベランダ発電かプラン最適化に振る

### 学ぶこと

kWh と W の違い、力率、待機電力の実測、ECHONET Lite / Wi-SUN（920MHz 帯の省電力無線）、時系列データの扱い（HA Recorder / InfluxDB、長期保存の設計）。

---

## セキュリティの最低ライン

**難しいことはしなくていい。だがこの 5 つを外すと自宅が踏み台になる。**
homelab の事故はほぼ全部「外に晒した」か「初期パスワードのまま」。

- [ ] **1. ルータのポートを開けない**（原則 3）— 「HA を外から使いたい」は**必ず** Tailscale で解決。DMZ も使わない
- [ ] **2. 初期パスワードを全部変える** — Proxmox `root` / Pi-hole / Grafana / HA。
      **危ないのは「LAN 内だから大丈夫」という思い込み**。感染端末が 1 台あれば内側から破られる
- [ ] **3. SSH のパスワード認証を切る**（Phase 0 で実施）
- [ ] **4. 自動更新を入れる**（`unattended-upgrades`）— 個人で一番現実的な防御は「放っておいても新しい」こと
- [ ] **5. バックアップを別の場所に置く** — ランサムウェアはネットワーク越しのバックアップも暗号化する。
      **月 1 回は外付け HDD を物理的に抜く**。これが 3-2-1 の「1」

### 外から使う — Tailscale と Cloudflare Zero Trust

**原則 3 は変えない。** その上で外から届かせる方法は 2 つあり、**用途が違う**。どちらも無料枠で足りる。

| | Tailscale | Cloudflare Zero Trust（Tunnel + Access） |
|---|---|---|
| 仕組み | 端末同士を WireGuard で直結（P2P） | 家から Cloudflare へ**外向き**に接続し、その道を逆流させる |
| 使う側 | **アプリの導入とログインが要る** | **ブラウザだけ**。URL を開くと認証画面 |
| 通信の中身 | 端末間で暗号化。**誰も中を見られない** | Cloudflare が TLS を終端 = **平文が一度 Cloudflare を通る** |
| ポート開放 | 不要 | 不要（接続は必ず内→外） |
| 無料枠 | Personal: 3 ユーザー / 100 デバイス | 50 ユーザーまで |
| 向く用途 | **自分の端末→自分のサービス**（SSH / Proxmox / Grafana） | **他人に URL を渡す**（家族・実家に見せる Immich） |

**判断は 1 行 — 「相手にアプリを入れさせられるか」。** 入れさせられるなら Tailscale が最善（誰も中身を見られない）。無理な相手に届けたいときだけ Cloudflare。

#### 導入するなら（Phase 2 以降）

- [ ] ドメインの DNS を Cloudflare に移す（Phase 2 の DNS-01 でどのみち必要）
- [ ] `cloudflared` を Docker で 1 個動かす（**ルータは一切触らない**）
- [ ] Cloudflare Access で「このメールアドレスだけ」のポリシーを作る
      — ⚠️ **Tunnel だけ張って Access を付けないのは、ただの全世界公開**。最大の事故ポイント
- [ ] 管理画面（Proxmox / Grafana / Pi-hole）は**絶対に Tunnel に載せない**
- [ ] （任意）**Gateway** + WARP をスマホに入れる → **家の外でも広告ブロックが効く**（Pi-hole は家の中だけ）
      - ⚠️ 無料枠の目安は 1 シート月 15 万クエリ。個人で超えることはまずないが、上限はゼロではない

#### ⚠️ ここが一番怖い

Cloudflare を挟むと、**ドメイン全体の鍵を 1 社に預ける**（DNS + 証明書 + 認証）。Tailscale だけなら、それが落ちても家の中は動く。**依存を寄せるほど自分で直せない障害が増える。**

- [ ] 家の中からのアクセスは **Cloudflare を通さず内部 DNS で直接**引くようにしておく
      （でないと回線が切れた瞬間に家中の照明が操作できなくなる）

### やらなくていいこと

凝ったファイアウォールルール（`ufw` で足りる）、WAF、多段プロキシ、証明書の相互認証。**外部から届かないなら、そこは攻撃面ではない。** Cloudflare Access を自分専用に使うのも不要（依存が増えるだけ）。

---

## サービスカタログ（何を載せるか）

**一度に全部入れると必ず破綻する。** 「今ある不満」を解決するものを 2〜3 個選び、安定させてから次へ。

### 先に入れる（生活インパクト順）

- [ ] **Pi-hole** — 全端末から広告が消える
- [ ] **Vaultwarden** — パスワード管理。Bitwarden 公式アプリがそのまま使える（→ 有料プラン解約）
- [ ] **Homepage** — 全サービスの入口。**IP とポートを覚えなくて済む**
- [ ] **Immich** — 写真が自分のものになる（→ iCloud 解約）
- [ ] **ntfy** — 洗濯終了 / バックアップ成功 / 宅配到着をスマホへ。**地味に一番使う**

### 効いてくる（後から）

- [ ] **Paperless-ngx** — 書類を OCR + 全文検索。保証書・確定申告・契約書が 3 秒で出る。**地味に一番人生が変わる**
- [ ] **Syncthing** — 端末間ファイル同期。Nextcloud より圧倒的に軽い。同期だけならこちらが先
- [ ] **Jellyfin** — 手持ちの動画・音楽を全端末へ。完全無料（Plex と違い機能課金なし）
- [ ] **Actual Budget** — 家計簿。Phase 5 の電力データと並べると効く
- [ ] **Frigate**（玄関カメラ + 物体検知）— 便利だが CPU / 電力を食う。**やるなら最後**

### 小さな自動化（サービスではなく組み合わせ）

**スマートプラグ 1 個（2,000 円）**で消費電力を測ると、洗濯機・食洗機・炊飯器の状態が分かる。

- [ ] 消費電力が下がって 3 分続いたら → **洗濯終了を通知**（定番だが効果は最大級）
- [ ] 在宅検知（スマホの位置情報）× 温湿度 → 帰宅前にエアコン
- [ ] ドアセンサー 1 個 → 施錠忘れ通知

### 入れなくていい

`*arr` スタック、Gitea（GitHub で足りる）、**独自メールサーバー**（絶対にやめる。届かないメールのデバッグに人生を溶かす）。

---

## 家のネットワークを可視化する（フェーズ横断）

**低レイヤーの学習効率が一番高いのがここ。** 家に何が流れているかを見ると、ネットワークが図から実物に変わる。

### まず「なぜ見えないのか」

自分の PC で `tcpdump` を流しても、**家族のスマホの通信は 1 バイトも見えない**。理由はスイッチにある。

- スイッチは「どのポートにどの MAC が居るか」を**学習して表を持つ**（MAC アドレステーブル）
- 以降フレームは**宛先ポートにだけ**流れる。昔の**ハブ**は全ポートに垂れ流していた。スイッチはそれをやめた機械

つまり見る方法は原理的に 3 つ — **経路上に立つ / スイッチに複製させる / 各端末で測る**。下の Step はこれに対応する。

### Step 1 — DNS から見る（無料・Phase 1 で既に持っている）

- [ ] Pi-hole のクエリログを Grafana に流す
- **どの端末が、どのドメインを、何回引いたか**が出る。情報量とコストの比が全 Step で最高
- 「寝ている間もテレビが 5 分おきに通信している」はここで見つかる
- 学べること: DNS、名前解決の順序、なぜ端末が Pi-hole を迂回できるか（DoH / DoT）

### Step 2 — 回線品質を見る（無料・Phase 2）

- [ ] **speedtest-tracker**（速度）+ **smokeping**（遅延とパケットロスの推移）を立てる
- 「夜だけ遅い」を**グラフで**言える。ISP に問い合わせるときの唯一の武器
- 学べること: レイテンシ / ジッタ / パケットロスの違い、`mtr` の読み方

### Step 3 — 全トラフィックを見る（約 3,000 円・Phase 4）★本命

- [ ] **TP-Link TL-SG105E**（5 ポート、3,000 円前後）を買う。この値段でミラーポートと VLAN が使える
- [ ] `ルータ ── TL-SG105E ── 各端末` と繋ぎ、ミラー先ポートを homelab の NIC へ
- [ ] **ntopng Community Edition**（GPLv3・無料）で受ける — どの端末が、どのアプリで、どこへ、何 MB
- 学べること: ARP、MAC アドレステーブル、ブロードキャストドメイン、L2 と L3、SNI

### Step 4 — 経路上に立つ（Phase 4 後半・覚悟が要る）

- [ ] **OPNsense** を 2 NIC 機に入れ、自宅ルータを置き換える
- ntopng に加え **Suricata**（IDS = 既知の攻撃パターンと照合する仕組み）と VLAN 分離が使える
- ⚠️ **家族のインターネットが自分の実験対象になる**。原則 6 に真っ向から反する
- やるなら「元のルータに 5 分で戻せる」状態を確保してから。**平日夜にやらない**

### プライバシーの線引き（先に決める）

家族の通信を見ることになる。技術ではなく同意の問題。

- [ ] 見る前に**家族に伝える**。「セキュリティのため」ではなく「勉強のため」と正直に言う
- [ ] ログの**保存期間を決めて自動削除**する（例: 30 日）。集めっぱなしにしない
- HTTPS の**中身は見えない**（見えるのは接続先ドメインと通信量まで）。ここを誤解しない

---

## クラウドも同じリポジトリで管理する（フェーズ横断・Phase 2 から）

**「自宅は Terraform、クラウドは管理画面でポチポチ」は原則 1 を半分捨てている。**
個人のインフラは家の外にも散らばっている — ドメイン、DNS、VPN の ACL、GitHub の設定。**同じ `terraform/` に集める。**

**なぜやるか**: ① 1 年後の自分は必ず「この DNS レコード何だっけ」になる ② 個人インフラの棚卸しになり**解約に繋がる** ③ 仕事と違い**壊しても誰も困らない**（state を壊す練習ができるのは個人環境だけ）

### 入れるもの（全部無料枠）

| 対象 | プロバイダ | 管理するもの |
|---|---|---|
| **Proxmox** | `bpg/proxmox` | VM / LXC（Phase 2 で導入済み） |
| **Cloudflare** | `cloudflare/cloudflare` | DNS レコード、Tunnel、Access ポリシー |
| **Tailscale** | `tailscale/tailscale` | ACL、tag、鍵の有効期限 |
| **GitHub** | `integrations/github` | リポジトリ設定、ブランチ保護、Renovate |

**入れないもの**: アカウント自体（作成・課金）、料金が発生するクラウドリソース（**この homelab の目的はクラウド代を減らすこと**）、一度きりの手続き（ドメイン取得、B ルート申請 → `docs/` に書けば十分）。

### 進め方（既存を壊さない順番）

- [ ] `.gitignore` を**最初に**書く（state に API トークンが残る。**`terraform.tfstate` は絶対にコミットしない**。事故の 9 割はここ）
- [ ] 手で作ってあるものを **`import` ブロック**で取り込む
- [ ] **差分ゼロになるまで HCL を実物に合わせる**。ここを飛ばして `apply` すると本番の DNS が消える
- [ ] 差分ゼロを確認してから、以降の変更を Terraform 側だけで行う
- [ ] GitHub Actions で `terraform plan` を PR に出す（`apply` は手元で。個人リポジトリで自動 apply は不要）
- [ ] 認証情報は環境変数か SOPS。複数マシンから触りたくなったら Cloudflare R2（S3 互換・無料枠）を backend に。**困ってからでいい**

### 学ぶこと

**state とは何か**（現実と HCL の間の「前回こうだった」という記録でしかない）、`import` / `moved` / `removed`（**実物を消さずに Terraform から外す**方法）、API トークンのスコープ最小化、宣言的管理の限界（作れるが消せない / 差分が出続けるリソースに必ず出会う）。

---

## ハードウェア選定（2026 年 8 月時点）

### 結論

**新品 N150 ミニ PC（16GB / 512GB）をセールで 2 万円前後。これが最安かつ最短。**
中古 1L ミニ PC は「分解して増設もしたい」なら。**Wildcat Lake は待たない・買わない。**

| 候補 | 価格 | アイドル | 向き / 不向き |
|---|---|---|---|
| **新品 N150 ミニ PC**（GMKtec G3 Plus, AOOSTAR N1 Pro, BMAX B1 Pro 等） | **19,000〜25,000 円** | 7〜8W | ◎ **最安・最省電力・保証あり**、RAM 16GB + SSD 込みで即動く。△ 4 コア、RAM 上限 16GB の機種が多い |
| **中古 ThinkCentre M75q-1 / M720q Tiny** | 20,000〜25,000 円 | 8〜12W | ◎ RAM 64GB まで載る、**分解・増設しやすくハードの勉強に最適**、堅牢。△ 個体差・AC アダプタ・埃 |
| **新品 N350 / N355（8 コア）** | 30,000〜40,000 円 | 8〜10W | ◎ コア数が倍で TDP 9〜15W。**VM を 5 個以上並べるなら**。△ 1 台目には過剰 |
| **Core 3 300 番台（Wildcat Lake）** | 60,000 円〜 | 10W〜 | ◎ 速い。△ 搭載機が 10GbE / TB4 付きの高価格帯から。**1 台目には高すぎる** |
| **Minisforum MS-01 等** | 80,000 円〜 | 20W〜 | 10GbE・PCIe。**買わない** |
| **Raspberry Pi 5** | 15,000 円〜 | 4〜7W | ◎ GPIO / 電子工作。△ ARM・I/O が弱い。**Phase 4 で買う** |

### CPU 世代の現状

- **N100 と N150 は実質同性能**（マルチコア差 約 1%）。**安い方でいい**
- 一段上が **N350 / N355（Twin Lake, 8 コア）**。E コアのみで TDP 9〜15W。VM を並べるなら +1 万円の価値
- **Wildcat Lake（Core 3 300 番台）が 2026 年に登場。** Intel 18A・2P+4LPE、Core 3 304 の PassMark 約 11,500 = **N150（約 5,400）の 2 倍**
  - ただし **TDP 15〜35W** で「常時つけっぱなしで月 180 円」の前提から外れ、搭載機（Beelink EQi 等）は価格帯が一段上
  - **結論は変わらない — 待つ理由も買う理由もない。** Pi-hole と HA は N150 で余裕

### 安く買うコツ

- **定価で買わない。** N150 機は Amazon タイムセール / プライムデーで 1 万円近く落ちる。セール記事を 2 週間眺めてから買う
- **価格.com の最安値を鵜呑みにしない。** 上位は Win11 Pro 付き法人向けで 4〜5 万円。Proxmox を入れる以上**ライセンス代は捨て金**
- **中古は「OS なし / Windows なし」を狙う**。同じ個体でも数千円安い
- ベアボーン + RAM / SSD を自分で載せる方が安いことがある。**それ自体がハードの勉強になる**

### 判断基準（迷ったらこれ）

- **RAM 16GB 以上、できれば増設できる機種**。CPU よりメモリで詰まる（ローカル LLM をやるなら 32GB 欲しくなる）
- **メモリがオンボード直付けでないか確認**（SO-DIMM スロットの有無）。ここを外すと一生増設できない
- **有線 LAN 必須**（できれば 2.5GbE）。Wi-Fi でサーバーを運用しない
- **NVMe スロット + 2.5inch ベイ**があると後で困らない

### 中古を買うときのチェックリスト

- [ ] メモリスロットの数と上限（オンボードでないこと）
- [ ] AC アダプタが付属するか（専用形状は後から入手困難。USB-C PD なら理想）
- [ ] NVMe / 2.5inch ベイの空き
- [ ] Windows ライセンスは**不要**（その分安い個体でいい）
- [ ] 届いたらまずエアダスターで吸気口を清掃（埃はサーマルスロットリングの原因）
- [ ] **1 週間は負荷をかけて放置**してから本番投入（初期不良はここで出る）

**買わなくていいもの**: ラック、10GbE、サーバー用 CPU、UPS（Phase 4 まで）、複数台クラスタ。全部「困ってから」。

---

## コスト

| 項目 | 費用 |
|---|---|
| Proxmox / Ansible / Terraform / Docker / HA / Ollama / Open WebUI | 0 円 |
| Tailscale Personal / Cloudflare Zero Trust / GitHub | 0 円 |
| **電気代**（8W 24h・31 円/kWh） | **約 180 円/月** |
| 初期投資（新品 N150 ミニ PC・16GB/512GB） | **20,000 円前後** |
| Zigbee ドングル + センサー 1 個 | 5,000 円前後 |
| ミラーポート対応スイッチ（TL-SG105E） | 3,000 円前後 |

ドメインは Phase 2 の内部 HTTPS（DNS-01）で欲しくなる。**年 1,500 円程度**で、これだけは払う価値がある。不要なら HTTP or 自己署名でも進む。
Cloudflare Tunnel は**自分だけなら不要** — Tailscale で足りる。**他人に URL を渡す用が出てから**入れる。

**回収できる支出**（Phase 4-5、やるなら）

| 項目 | 費用 | 回収 |
|---|---|---|
| Immich で iCloud 200GB を解約 | HDD 代のみ | 年 4,800 円。**2 年で回収** |
| Wi-SUN ドングル（B ルート） | 5,000〜10,000 円 | 可視化による削減で数か月 |
| 太陽光 + 蓄電池 | 100 万円〜 | 10 年前後。**データを取ってから判断** |

### 電気代をどこまで下げられるか

**0 円にはできない。だが年 2,000 円台は「実質ゼロ」で、そもそも狙う指標が違う。**

8W × 24h × 365 = **70 kWh/年 ≒ 2,200 円/年**。これを太陽光で自給すると:

| 構成 | 費用 | 回収 |
|---|---|---|
| 100W パネル + 12V 50Ah LiFePO4 + MPPT | 約 26,000 円 | **12 年**（バッテリー寿命 ≒ 10 年） |
| 冬・雨天も安定させるなら 200W + 1kWh 級 | 10 万円超 | **40 年以上** |

**バッテリーの寿命が先に来る**ので経済的に成立しない。ただし**学習教材としては最高**（DC 電源、充放電制御、電力収支の設計）。やるなら「学ぶため」と割り切る。

### 現実的に削る手（ここは効く）

- **HDD を積まない** — 3.5 インチは 1 台 6〜8W で**本体と同じだけ食う**。SSD なら 0.5W。要るなら普段はスピンダウン
- `powertop --auto-tune` で C-state（CPU の省電力状態）を効かせる。数 W 変わる
- **Frigate を入れない** — 常時 CPU を回すので跳ねる
- **k3s を 3 台で組まない** — 電気代がそのまま 3 倍。学習なら 1 台に VM 3 つで足りる
- **LLM 用の GPU を常時挿さない** — アイドルだけで +15〜25W（**ミニ PC 3 台分**）
- ワットチェッカーで**実測してから**削る。推測で削らない

ここまでで 6〜7W = **月 150 円**。これが実質的な下限。

### 狙うべき指標: 電気代ではなく収支

| | 年額 |
|---|---|
| 電気代 | −2,200 円 |
| iCloud 200GB 解約（Immich） | +4,800 円 |
| パスワード管理の有料プラン解約（Vaultwarden） | +3,000〜5,000 円 |
| **合計** | **+5,600 円以上の黒字** |

**電気代 0 円を目指すより、Immich と Vaultwarden を動かす方が 2 倍速い。**

---

## リポジトリ構成

最初はこれだけ。増やさない。

```
homelab/
├── PLAN.md
├── ansible/          # inventory.yml / site.yml / roles/
├── compose/          # サービスごとの docker-compose.yml
├── terraform/        # Phase 2 から。自宅(Proxmox)とクラウド(Cloudflare/Tailscale/GitHub)を同居
└── docs/             # 学習ログ・詰まった記録
```

- Secrets は Ansible Vault か SOPS。**平文を Git に置かない**
- `docs/` は綺麗に書かない。「何が起きて、何が原因で、どう直したか」の 3 行でいい

---

## 低レイヤー学習トラック

フェーズを進めると自然に触るテーマ。**先に本を読まない。詰まってから該当箇所を読む。**

| 層 | いつ | 触るもの |
|---|---|---|
| プロセス / OS | Phase 0-1 | `systemd`, プロセスの親子, シグナル, exit code, `strace` |
| ファイルシステム | Phase 0-2 | パーティション, LVM, マウント, inode, `df` と `du` の差 |
| ネットワーク | Phase 1-4 | ARP, DNS, DHCP, NAT, `tcpdump`, ルーティングテーブル, VLAN, SNI |
| 仮想化 | Phase 2 | KVM, namespace / cgroup, virtio, ブリッジ |
| ストレージ | Phase 4 | ZFS, RAID, SMART, 書き込み耐性 (TBW) |
| ハードウェア | Phase 2-5 | TDP と実消費電力, SODIMM / DDR4-5, NVMe と SATA, 冷却 |
| 推論の下回り | Phase 4 | 量子化, KV キャッシュ, メモリ帯域, CPU と GPU の使い分け |
| 電気・電子 | Phase 4-5 | 電圧 / 電流 / 電力, GPIO, I2C, はんだ付け, 直流と交流 |
| カーネル境界 | 道場でのみ | syscall, `strace`, eBPF, TCP ハンドシェイク, パケットの実物 |

深掘りしたくなったら `deep-dive` skill で一次情報まで降りる。

---

## 道場（意図的に壊して直す）

**この PLAN の弱点は「うまくいくと何も学べない」こと。** 構築は最終的にドキュメント通りに動いてしまう。低レイヤーが身につくのは**壊れた瞬間**だけ。

月に 1 回、実験用 VM で 1 つ選んで壊す。所要 30 分〜。**直せなくてもいい。何が起きたかを説明できれば合格。**

毎回の手順: ① 壊す前に「何が起きると思うか」を `docs/` に 1 行 → ② 壊す → ③ **ログとエラーコードだけで**推測（すぐ検索しない）→ ④ 一次情報で答え合わせ → ⑤ 外れた箇所だけ 3 行で残す

### 壊す

- [ ] 1. `/etc/fstab` に存在しないディスクを書いて再起動 → 起動シーケンス、emergency shell、initramfs
- [ ] 2. ディスクを 100% 埋める（`fallocate`）→ inode と block の違い、ログが書けない systemd の挙動
- [ ] 3. メモリを食い潰すプロセスを動かす → OOM Killer、`exit 137`、cgroup のメモリ制限
- [ ] 4. デフォルトゲートウェイを消す → ルーティングテーブル、`ip route`、DNS と疎通の切り分け
- [ ] 5. Docker のポートを別サービスと衝突させる → bind、`ss -tulpn`、iptables の NAT テーブル
- [ ] 6. ディスクを引き抜く（実験用のみ）→ ファイルシステムの一貫性、fsck、SMART
- [ ] 7. 証明書の有効期限を切らす（時計を進める）→ TLS 検証、chain of trust、NTP の重要さ
- [ ] 8. **バックアップから全部復元する** → **これが本命。年 1 回は必ずやる**

### 覗く（壊さずに中を見る）

壊す課題だけだと**運用の低レイヤー**で止まる。カーネルとプロトコルの中身に触れるのはこの 3 つ。**各 1 回で十分。**

- [ ] 9. `tcpdump -i any port 53 -nn` を流しながらブラウザを開く → DNS が**何十本も飛んでいる**こと、Pi-hole のブロックが生で見える
- [ ] 10. Wireshark で HTTPS 接続を 1 本キャプチャ → TCP 3-way → TLS ClientHello → 暗号化。**どこから中身が見えなくなるか**が目で分かる
- [ ] 11. `strace -f -p $(pidof pihole-FTL)` を数秒 → サービスの正体は `read` / `write` / `epoll_wait` の繰り返しでしかない

さらに降りたくなったら `bpftrace`（eBPF = カーネルに安全に計測コードを差し込む仕組み）。
例: `bpftrace -e 'tracepoint:syscalls:sys_enter_openat { @[comm] = count(); }'` で「今どのプロセスが何回ファイルを開いているか」が一発で出る。

### 自分のコードを 1 個だけ置く

**この PLAN の最大の穴は、全部 YAML と HCL で終わること。** 設定を書くのとプログラムを書くのは使う脳が違う。

- [ ] 12. **Go で 100 行の exporter を書く** — 回線速度 / 室温 / 電気代を Prometheus 形式（`# HELP` と `# TYPE` が付いた**ただのテキスト**）で `:9100/metrics` に吐くだけ
- [ ] 13. 既存の exporter を読んでから真似る。**HTTP サーバーを書き → メトリクスを設計し → Grafana に出す**まで自分の手で

ここまでやると Prometheus が「魔法」から「定期的に HTTP GET してテキストを parse しているだけ」に変わる。

### 年 1 回の「リストア訓練」

- [ ] 14. ミニ PC のディスクを消し、**バックアップと Git だけ**から全復旧する。所要時間を測って記録する

これができれば「壊しても直せるインフラ」を持っていると言える。できなければ、それはまだ homelab ではなく**運任せの本番環境**。

---

## 最初の一歩（今日やること）

- [ ] `winver` で Windows のエディションを確認する
- [ ] Pro なら Hyper-V を有効化、Home なら VirtualBox を入れる
- [ ] Ubuntu Server LTS の ISO を落とす

ここまでで Phase 0 の半分が終わる。

### 詰まったときのルール（挫折対策）

**最大の失敗要因は技術の難しさではなく「飽きること」と「詰まって放置すること」。**

- **30 分詰まったら検索する**。意地を張らない（道場のときだけは別）
- **1 日で解決しなかったら寝る**。翌朝 5 分で解けることが本当に多い
- **手が止まったら「作る」をやめて「壊す」に切り替える**（→ 道場）。手を動かし続ける方が大事
- **1 週間触れなくても気にしない**。盆栽は毎日いじるものではない

---

## 参考

- [Proxmox VE 9.0 リリース（公式）](https://proxmox.com/en/about/company-details/press-releases/proxmox-virtual-environment-9-0)
- [bpg/terraform-provider-proxmox](https://github.com/bpg/terraform-provider-proxmox) / [Terraform Registry](https://registry.terraform.io/providers/bpg/proxmox/latest)
- [Home Assistant インストール（公式）](https://www.home-assistant.io/installation/) / [Ollama 統合（公式）](https://www.home-assistant.io/integrations/ollama/)
- [Tailscale 無料プラン（公式）](https://tailscale.com/docs/account/manage-plans/free-plans-discounts)
- [Cloudflare Zero Trust（公式）](https://developers.cloudflare.com/cloudflare-one/) / [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/)
- [Ansible ベストプラクティス（公式）](https://docs.ansible.com/ansible/latest/tips_tricks/index.html)
- [Terraform `import` ブロック（公式）](https://developer.hashicorp.com/terraform/language/import)
- [Cloudflare プロバイダ](https://registry.terraform.io/providers/cloudflare/cloudflare/latest/docs) / [Tailscale](https://registry.terraform.io/providers/tailscale/tailscale/latest/docs) / [GitHub](https://registry.terraform.io/providers/integrations/github/latest/docs)
- [Prometheus（公式）](https://prometheus.io/docs/introduction/overview/) / [node_exporter](https://github.com/prometheus/node_exporter)
- [Ollama（公式）](https://docs.ollama.com/) / [Open WebUI（公式）](https://docs.openwebui.com/)
- [Immich（公式）](https://immich.app/) / [Vaultwarden Wiki](https://github.com/dani-garcia/vaultwarden/wiki) / [Paperless-ngx](https://docs.paperless-ngx.com/) / [Homepage](https://gethomepage.dev/)
- [Caddy（公式）](https://caddyserver.com/docs/) / [ntopng（公式）](https://www.ntop.org/guides/ntopng/) / [OPNsense（公式）](https://docs.opnsense.org/) / [k3s（公式）](https://docs.k3s.io/)
- [ha-teamtracker（HACS）](https://github.com/vasqued2/ha-teamtracker)

### 先人の記録（読み物）

- [自宅サーバーに入門してみた（Zenn）](https://zenn.dev/sonicmoov/articles/246c02cb2857d0)
- [再:自宅サーバーを始めてみよう!（YouTube, 2020）](https://www.youtube.com/watch?v=XC904l9TV6Y)
